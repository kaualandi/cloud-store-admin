import { DragInDropDirective } from './drag-in-drop.directive';

describe('DragInDropDirective', () => {
  it('should create an instance', () => {
    const directive = new DragInDropDirective();
    expect(directive).toBeTruthy();
  });
});
